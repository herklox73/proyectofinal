{# templates/user/index.html.twig #}

<h1>Listado de Usuarios</h1>

<ul>
    {% for user in users %}
        <li><a href="{{ path('user_profile', {'id': user.id}) }}">{{ user.email }}</a></li>
    {% else %}
        <li>No hay usuarios disponibles.</li>
    {% endfor %}
</ul>
